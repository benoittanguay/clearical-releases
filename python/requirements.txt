# FastVLM Inference Server Dependencies

# Core dependencies
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
pydantic>=2.5.0

# MLX-VLM for FastVLM model inference
# Note: Requires Apple Silicon Mac
mlx-vlm>=0.0.9

# Model download and management
huggingface_hub>=0.20.0

# Build dependencies
pyinstaller>=6.0.0

# Optional: For better performance
# python-multipart  # For file uploads (if needed in future)
